<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="db.properties"></properties>
	
	<!-- 别名的定义 -->
		<typeAliases>
		<!-- 针对单个别名的定义。type：类型的路径； alias：别名 -->
		<typeAlias type="com.mybatis.po.User" alias="user"/>
		<typeAlias type="com.mybatis.po.Orders" alias="orders"/>
	</typeAliases>
	
	
		<!-- 和Spring整合后environment配置都会被干掉 -->
		<environments default="development">
			<environment id="development">
			<!-- 使用jdbc事务管理，目前由mybatis来管理 -->
			<transactionManager type="JDBC" />
			<!-- 数据库连接池，目前由mybatis来管理 -->
			<dataSource type="POOLED">
			<property name="driver" value="${driver}" />
			<property name="url" value="${url}" />
			<property name="username" value="${name}" />
			<property name="password" value="${password}" />
			</dataSource>
			</environment>
			</environments>
		<mappers>
			<mapper resource="com/mybatis/mapper/UserMapper.xml" />
			<mapper resource="com/mybatis/mapper/UserMapper2.xml" />
			<mapper resource="com/mybatis/mapper/UserMapperOrders.xml" />
		</mappers>
</configuration>
